#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
typedef string str;

//  ~~ * ~~ NGHIA HAS GH ~~ * ~~ //
//  ~~ * ~~ NGHIA belongs to GHL ~~ * ~~

const long long ghl = 15052007;
const long long anniversary = 11102022;
const bool i_love_you_so_much = true; 
const long long MOD = 1e9 + 7;
const long long MAX = 1e18;

// ~~ * ~~ * ~~ *  ~~ *  ~~ * ~~ * //


struct Point{
    long long x,y;
};

bool checklapping(Point a1, Point a2, Point b1, Point b2){
    if(a1.x >= b2.x || a2.x >= b1.x)    return false;
}
pair <Point, Point> str;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

  //  SOL GHL;
//    GHL.solve();

}