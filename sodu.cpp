#include <bits/stdc++.h>
using namespace std;

typedef long long type;
typedef string str;
long long mod;
struct Matrix {
    vector < vector < long long> > matx;
    
};

int main(){ 
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
}